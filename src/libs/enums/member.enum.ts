export enum MemberType {
  USER = "USER",
  OWNER = "OWNER",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

export enum MemberHwStatus {
  PROCESS = "PROCESS",
  APPROVE = "APPROVE",
  NOTCHECKED = "NOTCHECKED",
}
